# U1.3-OPS-R1 — ВАЛИДАЦИЯ ПЕРЕЗАПУСКА (RESTART)

**Метка времени:** 2026-07-26T01:26:49Z (зондирование), 2026-07-26T01:26:54Z (перезапуск)  
**Источники:** logs/05-continuous-probe.log, logs/06-rollout-restart.log

## Контекст

Выполнен `kubectl rollout restart deployment/aither-bff` с параллельным непрерывным HTTP-зондированием двух зон:

- **Internet Zone** — внешний DNS-запрос к публичному эндпоинту
- **Test Zone** — HTTP-зондирование сервиса aither-bff внутри кластера

Интервал зондирования: 1 с, общая длительность: 180 с.

## Предстартовое состояние

| Под | Статус | Возраст |
|-----|--------|---------|
| aither-bff-85f756f59-gd5gv | Running | 161m |

Ревизия: 15 (текущая).

## Выполнение перезапуска

```
kubectl rollout restart deployment/aither-bff
→ deployment.apps/aither-bff restarted
→ EXIT_CODE=0
```

Rollout status: новый под поднят за ~36 с, успешно:
```
deployment "aither-bff" successfully rolled out
→ EXIT_CODE=0
```

## Пост-перезапускное состояние

| Под | Статус | Возраст |
|-----|--------|---------|
| aither-bff-778cdf45b5-m4sng | Running | 36s |

Ревизия увеличена до 16 — перезапуск зафиксирован в истории.

## Результаты зондирования (окно перезапуска)

Начало зондирования: `2026-07-26T01:26:49Z`. Перезапуск запущен: `2026-07-26T01:26:54Z`.

### Internet Zone (внешний DNS)

**Весь период зондирования: `internet=000000`.** Код возврата curl:28 — DNS-таймаут. Внешний DNS-резолвер не отвечал на протяжении всех 180 с зондирования, вне зависимости от перезапуска.

| Время (UTC) | Internet | Примечание |
|-------------|:--------:|------------|
| 01:26:49 | 000000 | До перезапуска |
| 01:26:55 — 01:29:47 | 000000 | Весь период |

> **Важно:** `internet=000000` означает `curl:28` (CURLE_OPERATION_TIMEDOUT) — DNS-резолвер не ответил. Это **не связано** с перезапуском aither-bff; проблема на стороне внешнего DNS или сетевого доступа узла.

### Test Zone (внутрикластерный HTTP)

Сервис показал **502 Bad Gateway в течение ~43 с** в окне перезапуска, затем восстановился до 200.

| Время (UTC) | Test | Примечание |
|-------------|:----:|------------|
| 01:26:49 | 200 | До перезапуска — сервис жив |
| 01:26:55 | 502 | Окно недоступности началось |
| 01:27:01 | 502 | |
| 01:27:07 | 502 | |
| 01:27:14 | 502 | |
| 01:27:20 | 502 | |
| 01:27:26 | 502 | Последний 502 |
| 01:27:32 | 200 | Восстановление — новый под готов |
| 01:27:38 — 01:29:47 | 200 | Стабильно 200 |

**Окно недоступности:** ~43 с (с 01:26:55 по 01:27:26). Это соответствует времени, необходимому для завершения старого пода и готовности нового пода после `rollout restart`.

## Заключение

1. **Перезапуск aither-bff выполнен успешно** — новый под `aither-bff-778cdf45b5-m4sng` поднят за ~36 с, ревизия увеличена до 16.
2. **Internet Zone** — DNS-таймаут (`curl:28`) на всём протяжении зондирования. Проблема **не связана** с перезапуском aither-bff; внешний DNS-резолвер недоступен для узла кластера.
3. **Test Zone** — ожидаемое окно 502 (~43 с) в момент переключения подов, затем стабильные 200. Сервис корректно восстановился.
4. **Отсутствие ложных выводов:** DNS-таймаут Internet Zone — отдельная проблема сетевой инфраструктуры, а не следствие перезапуска сервиса.
