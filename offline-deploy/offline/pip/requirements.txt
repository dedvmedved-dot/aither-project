# Gateway Python-зависимости
# Версия: 1.2.0 — точное соответствие gateway/requirements.txt
# НЕ включает chromadb/sentence-transformers — они только в chroma-proxy (образ chromadb/chroma)
redis>=5.0
pyjwt>=2.8
psycopg2-binary>=2.9
pyyaml>=6.0
