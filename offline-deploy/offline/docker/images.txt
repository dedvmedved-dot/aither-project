# Docker images list — версия 1.2.0
# Образы, необходимые для развёртывания в закрытом контуре
# Сборка:  make bundle  (docker save в images.tar.gz)

# Инференс
vllm/vllm-openai:latest

# Gateway (собирается из gateway/Dockerfile, python:3.12-slim)
# Образ должен быть собран и запушен в локальный registry или ghcr.io
# docker build -t registry.local:5000/aither-gateway:latest gateway/
ghcr.io/dedvmedved-dot/aither-project-gateway:latest

# Инфраструктура
postgres:16
redis:7-alpine

# RAG (ChromaDB 0.5.23 — прокси использует тот же образ)
# ВАЖНО: 0.5.23, НЕ latest (0.6.3 — REST API сломан)
chromadb/chroma:0.5.23

# Мониторинг
prom/prometheus:latest
grafana/grafana:latest

# Портал (для сборки, не запускается в K8s)
node:20-alpine
nginx:alpine
