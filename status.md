# Статус проекта Aither — 08.07.2026

## Подключение

**Портал (проверка извне):**
```bash
curl http://130.17.1.90:80/health
curl http://130.17.1.90:80/api/v1/status
```

**VPS2 (130.17.1.90):**
```bash
ssh root@130.17.1.90
```

**40.51 — ядро (10.129.13.78):**
```bash
# Прямой SSH (VPS2 → 78): ping 51ms, sshpass -p 'root' ssh root@10.129.13.78
# Через .21: sshpass -p '!QAZxsw2123' ssh svlkravchuk@10.129.11.21 → sshpass -p 'root' ssh root@10.129.13.78
```

**Проверка ядра с VPS2:**
```bash
curl http://10.129.13.78:30900/health
curl http://10.129.13.78:32293/v1/models
```

**Проверка K8s на 40.51:**
```bash
ssh root@130.17.1.90 "sshpass -p 'root' ssh root@10.129.13.78 'kubectl get pods -A'"
```

**Интернет на 40.51 (проверено 08.07):**
```bash
# ✅ ya.ru (302), docker.io (404), pypi.org (200), github.com (200)
# Можно: pip install, docker pull, git clone
```
kubectl get pods -A
```

---

## Сводка компонентов (проверено 06.07.2026 23:52)

| # | Компонент | Адрес | Статус | Проверка |
|---|---|---|---|---|
| 1 | **nginx** | VPS2:80 | ✅ | HTTP 200, SPA |
| 2 | **BFF** | VPS2:3000 | ✅ | `/auth/dev/login` → 200, v0.5.0, 7 орг, 10 польз., 17 ключей |
| 3 | **PostgreSQL** | VPS2:5432 | ✅ | `pg_isready` — accepting |
| 4 | **vLLM Qwen2.5-14B** | 40.51:32293 | ✅ | `/v1/models` → модель загружена, TP=2 |
| 5 | **Gateway** | 40.51:30900 | ✅ | `/health` → `{"status":"ok","billing":"enabled"}` |
| 6 | **Gateway → vLLM** | внутренний | ⚠️ | `"vLLM error: 402"` — биллинг блокирует (нет токенов) |
| 7 | **BFF → Gateway (JWT)** | внутренний | ✅ | Делегирование работает |
| 8 | **BFF → Gateway → vLLM** | сквозной | ⚠️ | Чат создаётся (200), ответ модели — 402 (биллинг) |
| 9 | **SSH на 40.51** | root@10.129.13.78 | ✅ | Прямой: ping 51ms, sshpass; цепочка VPS2→.21→78 |
| 10 | **40.50** | — | ❌ | RAID сбой, CMOS 2001 г. |

### Вывод

Вся цепочка работает: **Браузер → nginx → BFF → Gateway → vLLM**. Единственный блокер — биллинг Gateway возвращает 402 (нет выделенных токенов организации). Нужно либо пополнить баланс в БД биллинга, либо временно отключить проверку в Gateway.

---

## Функционал: что реализовано / что нет

### Реализовано (Gates 0–10)

| Gate | Компонент | Где | Статус |
|------|-----------|-----|--------|
| 0 | NVIDIA 570 + Docker | 40.51 | ✅ |
| 1 | K8s single-node + Flannel 0.25.7 | 40.51 | ✅ |
| 2 | GPU Operator + vLLM + Qwen2.5-14B (TP=2) | 40.51 | ✅ |
| 3 | PostgreSQL 16 + Redis 7 + API Gateway + Billing | 40.51, K8s | ✅ |
| 4 | Portal BFF + Portal DB + nginx | VPS2 | ✅ |
| 5 | Dev-аутентификация + JWT | VPS2 | ✅ |
| 6 | Организации + участники | VPS2 | ✅ |
| 7 | API-ключи (ak-...) + ротация | VPS2 | ✅ |
| 8 | Billing (reserve→settle→refund) + Usage | 40.51 | ✅ |
| 9 | Портал SPA (дашборд, организации) | VPS2 | ✅ |
| 10 | Чат-портал (SSE streaming, история, share) | VPS2 | ✅ |
| — | JWT-делегирование BFF → Gateway | VPS2 → 40.51 | ✅ |

### Доработки чата (05.07.2026)

- ✅ Лейаут DeepSeek-style (3 независимые зоны)
- ✅ Удаление чатов
- ✅ Подсветка синтаксиса (python/bash/js/sql) — встроенная
- ✅ Авто-# для русских строк в коде
- ✅ Сохранение chatId в localStorage (не плодит пустые чаты)
- ✅ Изоляция чатов по user_id

### Модели — статус загрузки (06.07.2026)

| Модель | Размер | VPS2 | 40.51 | Статус |
|---|---|---|---|---|
| Qwen 14B | 28 GB | — | ✅ | В vLLM, работает |
| Saiga 8B | 14 GB | — | ✅ | Загружена, ждёт развёртки |
| Qwen Coder 14B | 29 GB | ✅ 6.3 GB | 🔄 tar | Копирование VPS2 → 40.51 |
| Qwen 32B GPTQ | 19 GB | 🔄 2.2 GB | ❌ | Заливка skhome01 → VPS2 |

---

## Изменения с последней проверки

| Было (документировано) | Стало (06.07.2026) |
|---|---|
| Gateway «требует доработки» | **Работает** — health ok, billing enabled |
| BFF → прямой vLLM (NodePort 32293) | **BFF → Gateway** (JWT-делегирование работает) |
| SSH root/root | **Заблокирован** — нужен сброс через BMC |
| Модель Qwen3-14B | **Qwen2.5-14B-Instruct** (факт) |

---

## Блокеры

| # | Проблема | Приоритет | Решение |
|---|---|---|---|
| 1 | Gateway возвращает 402 (нет токенов биллинга) | 🔴 | Пополнить баланс org_id=1 в БД биллинга |
| 2 | SSH заблокирован на 40.51 | 🟢 Исправлен | Прямой доступ: `sshpass -p root` + цепочка через .21 |
| 3 | 40.50 — RAID сбой, CMOS 2001 | 🟡 | Ремонт/замена оборудования |
| 4 | Интернет на 40.51 | 🟢 Проверен | ya.ru, docker.io, pypi.org, github.com — доступны |

---

## Дорожная карта

### ✅ Выполнено — Gates 0–10 (MVP)

Все 10 контрольных точек пройдены. Портал на http://130.17.1.90, чат работает, биллинг считает токены. JWT-делегирование BFF → Gateway настроено.

### 🔴 Ближайшие — разблокировка

| Задача | Приоритет | Где |
|---|---|---|
| Восстановить SSH на 40.51 | P0 | BMC 40.51 |
| Пополнить баланс организации в биллинге | P0 | 40.51, PostgreSQL |
| Проверить/обновить K8s-поды | P0 | 40.51 |

### 🔄 В работе — расширение модельного парка

| Задача | Приоритет | Где |
|---|---|---|
| Завершить копирование Coder 14B → 40.51 | P0 | 40.51 |
| Завершить заливку 32B GPTQ → VPS2 → 40.51 | P0 | skhome01 → VPS2 |
| Развернуть все модели в vLLM (мульти-модель) | P0 | 40.51 |
| Обновить Gateway + UI для выбора модели в чате | P0 | VPS2 + 40.51 |

### P1 — бизнес-логика

| Функция | Где |
|---|---|
| Rate Limiter (per-key, per-org) | 40.51 |
| Пополнение баланса (платёжный шлюз) | 40.51 |

### P2 — демонстрационный пакет

| Функция | Где |
|---|---|
| mTLS Portal BFF ↔ Core | VPS2 ↔ 40.51 |
| Email-уведомления (SMTP) | VPS2 |
| Аналитика потребления (графики, CSV) | VPS2 |
| Брендирование «Гринатом» (завершить) | VPS2 |

### P3 — отказоустойчивость и развитие

| Функция | Где |
|---|---|
| Админ-панель | 40.51 |
| Восстановление 40.50 (второй узел K8s) | 40.50 |
| Мультимодальная модель (Qwen2.5-VL-7B) | 40.51 |
| Диссертационное/патентное ядро | — |

---

## Архитектура

```
Клиент → http://130.17.1.90:80 (nginx)
              ↓
       Portal BFF :3000 ─── Portal DB :5432 (орг, польз., ключи, чаты)
              │
              ↓ JWT delegation (работает ✅)
    10.129.13.78:30900 (Gateway) ─── Billing DB (PostgreSQL + Redis)
              │
              ↓ ⚠️ 402 — нет токенов организации
         vLLM :8000 (Qwen2.5-14B, TP=2)
```

## Репозиторий

`dedvmedved-dot/aither-project` — 73 коммита, ветка main
