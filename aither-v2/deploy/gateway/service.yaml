apiVersion: v1
kind: Service
metadata:
  name: aither-gateway
  namespace: aither-inference
  labels:
    app: aither-gateway
    change: CHANGE-0022
spec:
  type: ClusterIP
  selector:
    app: aither-gateway
  ports:
  - name: http
    port: 8000
    targetPort: http
    protocol: TCP
