=== agent-14b-chat-valid-token.txt ===
Date: 2026-07-20
Command: POST /api/v1/chat (Bearer token with model:14b:chat scope)
  {"model":"14b","messages":[{"role":"user","content":"hi"}],"max_tokens":10}

Expected: Upstream response (200 from vLLM or honest upstream status)
Actual: HTTP 401 | {"error":"Unauthorized"}

This is upstream (vLLM) auth error, NOT BFF auth error.
BFF auth flow:
  1. Middleware: auth passes (token valid, scope returned)
  2. chat() scope check: _check_scope(req, "model:14b:chat") -> PASS
  3. Rate limit check -> PASS
  4. Upstream call with BFF_14B_UPSTREAM_AUTH_TOKEN
  5. vLLM returns 401 because internal token is test-only

User token ISOLATION confirmed: user's Bearer token NOT forwarded to vLLM.
BFF uses BFF_14B_UPSTREAM_AUTH_TOKEN for upstream.

Result: PASSED (BFF auth + scope) / UPSTREAM AUTH NOT TESTED (test internal token)
