=== agent-wrong-token-401.txt ===
Date: 2026-07-20
Command:
  curl -s -w "\nHTTP_CODE:%{http_code}" -H "Authorization: Bearer athr_invalidtoken" \
    http://aither-bff.aither-inference.svc:8000/api/v1/models

Expected: HTTP 401 {"error":"Token not found or revoked"}
Flow:
  1. Auth middleware intercepts -> auth_req()
  2. auth_req() extracts Bearer token -> htok() -> load_tm(token_hash)
  3. load_tm() does rcli.get(tmk(th)) -> None (token doesn't exist)
  4. Returns False -> middleware returns 401

Invalid token format (no athr_ prefix):
  - _verify_token() checks startswith("athr_") -> False
  - auth_req() falls through -> returns False

Actual: NOT COLLECTED — VPN unstable.
Result: NOT COLLECTED (LOGIC VERIFIED by source code review)
