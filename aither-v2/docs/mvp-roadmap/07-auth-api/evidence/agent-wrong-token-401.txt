=== agent-wrong-token-401.txt ===
Date: 2026-07-20
Command: GET /api/v1/models (Bearer: athr_invalid123)

Expected: HTTP 401 {"error":"Token not found or revoked"}
Actual: HTTP 401 | {"error":"Token not found or revoked"}

Auth flow for invalid token:
  1. Middleware calls auth_req()
  2. auth_req() extracts Bearer -> htok(invalid) -> load_tm()
  3. load_tm() does rcli.get(tmk(hash)) -> None
  4. Returns False -> middleware returns 401

Wrong format (no athr_ prefix) also handled by htok() but would produce incorrect hash.

Result: PASSED
