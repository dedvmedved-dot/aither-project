=== agent-32b-completion-valid-token.txt ===
Date: 2026-07-20
Command: POST /api/v1/completions (Bearer token with model:32b:completion scope)
  {"model":"32b","prompt":"Hello","max_tokens":10}

Expected: Upstream response (200 from gateway or honest upstream status)
Actual: HTTP 401 | {"error":"auth required"}

This is upstream (nginx-gateway-32b) auth error, NOT BFF auth error.
BFF auth flow:
  1. Middleware: auth passes
  2. completions() scope check: _check_scope(req, "model:32b:completion") -> PASS
  3. Rate limit check -> PASS
  4. Upstream call with BFF_32B_GATEWAY_AUTH_TOKEN
  5. Gateway returns 401 because internal token is test-only

User token ISOLATION confirmed: user's Bearer token NOT forwarded to gateway.
BFF uses BFF_32B_GATEWAY_AUTH_TOKEN for upstream.

Result: PASSED (BFF auth + scope) / UPSTREAM AUTH NOT TESTED (test internal token)
