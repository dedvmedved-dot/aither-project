=== no-user-token-forwarding-check.txt ===
Date: 2026-07-20
Method: Source code review of tools/bff/app.py

Verification:
  - grep "Authorization" aither-v2/tools/bff/app.py shows:
    1. req.headers.get("authorization") — for rate limit key generation
    2. req.headers.get("authorization") — in auth_req() for token extraction
    3. In chat(): headers = {"Authorization": "Bearer {ua(model)}"} — uses internal BFF token
    4. In completions(): same pattern — uses internal BFF token

  - Old _headers() function from v0.3.0 that forwarded user Authorization is REMOVED.
  - _upstream_headers() uses BFF_14B_UPSTREAM_AUTH_TOKEN or BFF_32B_GATEWAY_AUTH_TOKEN.
  - User token is used ONLY for BFF auth verification, NEVER for upstream call.

Conclusion: User API token is NOT forwarded to upstream vLLM or gateway.
Result: PASSED (source code review, verified with git grep)
