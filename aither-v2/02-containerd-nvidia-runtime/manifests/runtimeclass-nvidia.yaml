apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
  annotations:
    description: "NVIDIA GPU container runtime via containerd"
handler: nvidia
scheduling:
  nodeSelector:
    nvidia.com/gpu.present: "true"
