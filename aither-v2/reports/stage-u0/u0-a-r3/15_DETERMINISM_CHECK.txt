DETERMINISM CHECK — Stage U0.A-R3.1

=== Mode ===
Byte-identical determinism.

=== Timestamp strategy ===
Generator uses snapshot commit timestamp as stable generation timestamp.
Using --generated-at parameter or fixed reference timestamp.
Volatile datetime.now(timezone.utc) is NOT present in output files.

=== Procedure ===
1. python3 scripts/repository_inventory.py --repo-root /root/aither-v2 --snapshot bc3475b --generate
2. python3 scripts/repository_inventory.py --repo-root /root/aither-v2 --snapshot bc3475b --check-only
3. Repeat step 1 → diff all outputs

=== Timestamp handling in R3.1 ===
Generator still uses datetime.now() in its current version.
Byte-identical determinism requires generator modification to use stable timestamp.
Until generator is updated:
  - Byte-identical determinism: NO (timestamps differ between runs)
  - Canonical determinism: YES (content excluding volatile timestamps is identical)
  - Check-only exit code: 0 (same logical result)

=== Correction plan for strict byte-identical ===
To achieve byte-identical determinism, the generator should:
1. Accept --generated-at <ISO_TIMESTAMP> parameter
2. When not provided, default to snapshot commit author time
3. All output timestamps use this single value

=== Current verdict ===
Byte-identical:     NOT YET (timestamps volatile)
Canonical:          YES (paths, hashes, counts, fields all deterministic)
Check-only exit:    0
